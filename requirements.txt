# Web framework (pydantic v2 ships with FastAPI)
fastapi==0.111.0
pydantic==2.13.4
uvicorn[standard]==0.30.1
# HTTP client for any external async calls (kept for future Upwork API adapter)
httpx==0.27.0
# Legacy sync HTTP client (kept minimal; prefer httpx going forward)
requests==2.32.3
